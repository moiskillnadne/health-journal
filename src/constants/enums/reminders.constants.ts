export enum ReminderType {
  Once = 'once',
  Repeat = 'repeat',
}

export enum ReminderPeriod {
  Day = 'day',
  Week = 'week',
  Month = 'month',
  Year = 'year',
}

export enum ReminderTimePeriod {
  Second = 'second',
  Minute = 'minute',
  Hour = 'hour',
}
